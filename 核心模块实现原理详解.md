# 核心模块实现原理详解

## 一、评论模块

### 1.1 功能概述

评论模块支持用户对景点、美食、酒店、文化、攻略、体验项目等多种内容进行评论和评分，同时支持评论回复、点赞、删除等功能。

### 1.2 核心实体设计

**Comment 实体类** ([Comment.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/entity/Comment.java))：

```java
@Data
@TableName("comment")
public class Comment {
    private Long id;                    // 评论ID
    private Long userId;                // 用户ID
    private String targetType;          // 评论对象类型：attraction/food/hotel/culture/strategy/experience
    private Long targetId;              // 评论对象ID
    private String content;             // 评论内容
    private BigDecimal rating;          // 评分（0-5）
    private String ratingDetail;        // 多维度评分（JSON格式）
    private String images;              // 评论图片（JSON数组）
    private Integer likeCount;          // 点赞数
    private Integer replyCount;         // 回复数
    private Integer status;             // 状态：0-待审核，1-已通过
    private Integer isTop;              // 是否置顶
}
```

**设计特点**：
- **多态设计**：通过 `targetType` 和 `targetId` 支持多种内容类型的评论
- **冗余统计字段**：`commentCount`、`rating` 直接存储在内容表中，提高查询效率

### 1.3 添加评论流程

**核心实现** ([CommentServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/CommentServiceImpl.java#L56-L79))：

```java
@Transactional(rollbackFor = Exception.class)
public Comment addComment(Long userId, String targetType, Long targetId, 
                          String content, BigDecimal rating, 
                          String ratingDetail, String images) {
    // 1. 验证评论对象是否存在
    validateCommentTarget(targetType, targetId);
    
    // 2. 创建评论实体
    Comment comment = new Comment();
    comment.setUserId(userId);
    comment.setTargetType(targetType);
    comment.setTargetId(targetId);
    comment.setContent(content);
    comment.setRating(rating);
    comment.setRatingDetail(ratingDetail);
    comment.setImages(images);
    comment.setStatus(1);  // 默认通过审核
    comment.setLikeCount(0);
    comment.setReplyCount(0);
    
    // 3. 保存评论
    commentMapper.insert(comment);
    
    // 4. 更新对应内容的评论数和评分
    updateTargetCommentCount(targetType, targetId, rating);
    
    return comment;
}
```

### 1.4 评分更新算法

**核心实现** ([CommentServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/CommentServiceImpl.java#L178-L249))：

```java
private void updateRating(Object entity, BigDecimal newRating, String type) {
    // 获取当前评论数和评分
    int commentCount = getCommentCount(entity);
    BigDecimal currentRating = getCurrentRating(entity);
    
    // 计算新的平均分
    // 公式：(当前平均分 * (评论数-1) + 新评分) / 评论数
    if (commentCount > 0) {
        BigDecimal newAvg = currentRating.multiply(BigDecimal.valueOf(commentCount - 1))
                .add(newRating)
                .divide(BigDecimal.valueOf(commentCount), 2, RoundingMode.HALF_UP);
        setRating(entity, newAvg);
    } else {
        // 第一条评论
        setRating(entity, newRating);
    }
}
```

### 1.5 评论列表查询

**分页查询实现** ([CommentServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/CommentServiceImpl.java#L252-L275))：

```java
public PageVO<CommentVO> getCommentList(String targetType, Long targetId, 
                                        Integer page, Integer size) {
    // 1. 构建查询条件
    LambdaQueryWrapper<Comment> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Comment::getTargetType, targetType);
    wrapper.eq(Comment::getTargetId, targetId);
    wrapper.eq(Comment::getStatus, 1);
    wrapper.orderByDesc(Comment::getIsTop);        // 置顶优先
    wrapper.orderByDesc(Comment::getCreateTime);  // 最新优先
    
    // 2. 分页查询
    Page<Comment> pageParam = PageUtil.createPage(page, size);
    Page<Comment> result = commentMapper.selectPage(pageParam, wrapper);
    
    // 3. 转换为VO，填充用户信息
    List<CommentVO> voList = result.getRecords().stream()
            .map(this::convertToVO)  // 填充用户名、头像等
            .collect(Collectors.toList());
    
    return new PageVO<>(result.getTotal(), result.getPages(), 
                       result.getCurrent(), result.getSize(), voList);
}
```

### 1.6 删除评论

**级联删除实现** ([CommentServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/CommentServiceImpl.java#L311-L378))：

```java
@Transactional(rollbackFor = Exception.class)
public void deleteComment(Long commentId, Long userId) {
    Comment comment = commentMapper.selectById(commentId);
    // 1. 验证权限
    if (!comment.getUserId().equals(userId)) {
        throw new BusinessException("无权删除此评论");
    }
    
    // 2. 减少对应内容的评论数
    decreaseTargetCommentCount(comment.getTargetType(), comment.getTargetId());
    
    // 3. 删除该评论的所有回复
    LambdaQueryWrapper<CommentReply> replyWrapper = new LambdaQueryWrapper<>();
    replyWrapper.eq(CommentReply::getCommentId, commentId);
    commentReplyMapper.delete(replyWrapper);
    
    // 4. 删除评论
    commentMapper.deleteById(commentId);
}
```

---

## 二、AI模块

### 2.1 功能概述

AI模块包含两个核心功能：
1. **AI智能助手**：支持自然语言对话，提供旅游咨询服务
2. **AI攻略生成**：根据用户需求自动生成个性化旅游攻略

### 2.2 AI服务基础实现

**DeepSeek API调用** ([AiServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/AiServiceImpl.java#L38-L113))：

```java
public String generateContent(String prompt) {
    // 1. 检查AI服务是否启用
    if (!enabled || apiKey == null || apiKey.isEmpty()) {
        throw new RuntimeException("AI服务未启用或API密钥未配置");
    }
    
    try {
        String url = baseUrl + "/v1/chat/completions";
        
        // 2. 构建请求头（API密钥认证）
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        headers.setBearerAuth(apiKey);
        
        // 3. 构建请求体
        Map<String, Object> requestBody = new HashMap<>();
        requestBody.put("model", "deepseek-chat");
        
        Map<String, String> message = new HashMap<>();
        message.put("role", "user");
        message.put("content", prompt);
        requestBody.put("messages", new Object[]{message});
        
        requestBody.put("temperature", 0.7);      // 温度参数，控制随机性
        requestBody.put("max_tokens", 2000);     // 最大Token数
        
        HttpEntity<Map<String, Object>> request = new HttpEntity<>(requestBody, headers);
        
        // 4. 调用DeepSeek API
        ResponseEntity<String> response = restTemplate.exchange(
            url, HttpMethod.POST, request, String.class);
        
        // 5. 解析响应
        if (response.getStatusCode() == HttpStatus.OK && response.getBody() != null) {
            JSONObject jsonResponse = JSON.parseObject(response.getBody());
            if (jsonResponse.containsKey("choices")) {
                JSONObject choice = jsonResponse.getJSONArray("choices").getJSONObject(0);
                String content = choice.getJSONObject("message").getString("content");
                return content;
            }
        }
        
        throw new RuntimeException("AI服务返回异常");
    } catch (Exception e) {
        logger.error("调用AI服务失败", e);
        throw new RuntimeException("调用AI服务失败：" + e.getMessage(), e);
    }
}
```

### 2.3 AI智能助手实现

**对话流程** ([AiAssistantServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/AiAssistantServiceImpl.java#L60-L114))：

```java
public String chat(String message, List<Map<String, String>> conversationHistory) {
    // 1. 构建系统提示词（包含平台数据）
    String systemPrompt = buildSystemPrompt();
    
    // 2. 检测用户是否询问特定酒店，动态查询酒店详细信息
    String additionalHotelInfo = extractHotelInfoFromMessage(message);
    
    // 3. 构建完整对话上下文
    StringBuilder promptBuilder = new StringBuilder();
    promptBuilder.append(systemPrompt).append("\n\n");
    
    if (additionalHotelInfo != null && !additionalHotelInfo.isEmpty()) {
        promptBuilder.append("## 用户询问的酒店详细信息\n");
        promptBuilder.append(additionalHotelInfo).append("\n\n");
    }
    
    // 4. 添加对话历史
    if (conversationHistory != null && !conversationHistory.isEmpty()) {
        promptBuilder.append("以下是之前的对话历史：\n");
        for (Map<String, String> history : conversationHistory) {
            String role = history.get("role");
            String content = history.get("content");
            if ("user".equals(role)) {
                promptBuilder.append("用户：").append(content).append("\n");
            } else if ("assistant".equals(role)) {
                promptBuilder.append("助手：").append(content).append("\n");
            }
        }
    }
    
    // 5. 添加当前用户消息
    promptBuilder.append("用户：").append(message).append("\n");
    promptBuilder.append("助手：");
    
    // 6. 调用AI服务获取回复
    return aiService.generateContent(promptBuilder.toString());
}
```

**系统提示词构建** ([AiAssistantServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/AiAssistantServiceImpl.java#L405-L442))：

```java
private String buildSystemPrompt() {
    // 1. 获取平台数据摘要（景点、美食、酒店、活动公告、攻略）
    String dataSummary = getWebsiteDataSummary();
    
    // 2. 构建完整提示词
    return "你是一个专业的旅游助手，专门为家乡文旅宣传平台提供咨询服务。\n\n" +
           "你的职责是：\n" +
           "1. 回答用户关于旅游景点、美食、酒店、攻略、活动公告等相关问题\n" +
           "2. 根据用户需求推荐合适的旅游方案\n" +
           "3. 帮助用户规划旅游路线\n" +
           "4. 提供旅游建议和注意事项\n" +
           "5. 介绍最新的活动公告和优惠信息\n\n" +
           "以下是平台当前的数据信息，请基于这些信息回答用户的问题：\n\n" +
           dataSummary + "\n\n" +
           "重要注意事项：\n" +
           "- 回答要准确、友好、专业，必须基于上述数据信息回答\n" +
           "- 当用户询问酒店信息时，必须详细说明房型、面积、床型、价格等信息\n" +
           "- 如果用户询问的信息不在上述数据中，可以基于常识回答，但要明确说明\n";
}
```

### 2.4 AI攻略生成实现

**攻略生成流程** ([StrategyGenerateServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/StrategyGenerateServiceImpl.java#L48-L79))：

```java
public Strategy generateStrategy(StrategyGenerateDTO dto, Long userId) {
    // 1. 获取平台数据作为上下文
    List<Attraction> attractions = getAttractions();  // 20个热门景点
    List<Food> foods = getFoods();                    // 15个热门美食
    List<Hotel> hotels = getHotels();                // 10个热门酒店
    
    // 2. 构建提示词
    String prompt = promptTemplateBuilder.buildStrategyPrompt(dto, attractions, foods, hotels);
    
    // 3. 调用AI生成攻略
    String aiResponse = aiService.generateContent(prompt);
    
    // 4. 解析AI返回的内容并构建攻略对象
    return parseAiResponse(aiResponse, dto, userId);
}
```

**AI响应解析** ([StrategyGenerateServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/StrategyGenerateServiceImpl.java#L108-L186))：

```java
private Strategy parseAiResponse(String aiResponse, StrategyGenerateDTO dto, Long userId) {
    Strategy strategy = new Strategy();
    
    try {
        // 处理markdown代码块包裹（AI可能返回```json ... ```格式）
        String jsonStr = aiResponse.trim();
        if (jsonStr.startsWith("```json")) {
            jsonStr = jsonStr.substring(7);
        }
        if (jsonStr.startsWith("```")) {
            jsonStr = jsonStr.substring(3);
        }
        if (jsonStr.endsWith("```")) {
            jsonStr = jsonStr.substring(0, jsonStr.length() - 3);
        }
        jsonStr = jsonStr.trim();
        
        // 解析JSON
        JSONObject json = JSON.parseObject(jsonStr);
        strategy.setTitle(json.getString("title"));
        strategy.setDescription(json.getString("description"));
        strategy.setContent(json.getString("content"));
        strategy.setTips(json.getString("tips"));
        
        // 处理最佳季节，限制长度
        String bestSeason = json.getString("bestSeason") != null ? 
            json.getString("bestSeason") : dto.getBestSeason();
        if (bestSeason != null && bestSeason.length() > 50) {
            bestSeason = bestSeason.substring(0, 50);
        }
        strategy.setBestSeason(bestSeason);
        
        // 设置其他字段
        strategy.setDuration(dto.getDuration());
        strategy.setBudget(dto.getBudget());
        strategy.setTheme(dto.getTheme());
        strategy.setUserId(userId);
        strategy.setStatus(0);  // 待审核
        
        // 根据天数设置分类
        if (dto.getDuration() == 1) {
            strategy.setCategory("1day");
        } else if (dto.getDuration() == 2) {
            strategy.setCategory("2day");
        } else {
            strategy.setCategory("theme");
        }
        
    } catch (Exception e) {
        // JSON解析失败时的降级处理：使用原始内容
        logger.error("解析AI返回内容失败", e);
        strategy.setTitle("AI生成的" + dto.getDuration() + "日游攻略");
        strategy.setDescription("AI智能生成的旅游攻略，请查看详细内容");
        strategy.setContent(aiResponse);
        strategy.setDuration(dto.getDuration());
        strategy.setBudget(dto.getBudget());
        strategy.setTheme(dto.getTheme());
        strategy.setUserId(userId);
        strategy.setStatus(0);
        
        if (dto.getDuration() == 1) {
            strategy.setCategory("1day");
        } else if (dto.getDuration() == 2) {
            strategy.setCategory("2day");
        } else {
            strategy.setCategory("theme");
        }
    }
    
    return strategy;
}
```

---

## 三、地图模块

### 3.1 功能概述

地图模块提供以下功能：
1. **统一地图数据接口**：整合景点、美食、酒店、文化的地理位置
2. **地址自动解析**：地址转经纬度（地理编码）
3. **路径规划**：驾车、步行、骑行路线
4. **地点搜索**：关键词搜索POI

### 3.2 统一地图数据接口

**多类型数据整合** ([MapServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/MapServiceImpl.java#L61-L195))：

```java
// 获取景点地图数据
public List<Map<String, Object>> getAttractionMapData() {
    LambdaQueryWrapper<Attraction> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Attraction::getStatus, 1);
    wrapper.isNotNull(Attraction::getLongitude);  // 只查询有经纬度的
    wrapper.isNotNull(Attraction::getLatitude);
    List<Attraction> attractions = attractionMapper.selectList(wrapper);
    
    return attractions.stream().map(attraction -> {
        Map<String, Object> data = new HashMap<>();
        data.put("id", attraction.getId());
        data.put("name", attraction.getName());
        data.put("longitude", attraction.getLongitude());
        data.put("latitude", attraction.getLatitude());
        data.put("address", attraction.getAddress());
        data.put("coverImage", attraction.getCoverImage());
        data.put("type", "attraction");
        return data;
    }).collect(Collectors.toList());
}

// 获取餐厅地图数据（从美食的推荐餐厅JSON字段中解析）
public List<Map<String, Object>> getRestaurantMapData() {
    List<Map<String, Object>> restaurantList = new ArrayList<>();
    
    LambdaQueryWrapper<Food> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Food::getStatus, 1);
    wrapper.isNotNull(Food::getRecommendedRestaurants);
    List<Food> foods = foodMapper.selectList(wrapper);
    
    for (Food food : foods) {
        try {
            if (food.getRecommendedRestaurants() != null && !food.getRecommendedRestaurants().isEmpty()) {
                JSONArray restaurants = JSON.parseArray(food.getRecommendedRestaurants());
                for (int i = 0; i < restaurants.size(); i++) {
                    JSONObject restaurant = restaurants.getJSONObject(i);
                    if (restaurant.containsKey("longitude") && restaurant.containsKey("latitude")) {
                        Map<String, Object> data = new HashMap<>();
                        data.put("id", "restaurant_" + food.getId() + "_" + i);
                        data.put("name", restaurant.getString("name") != null ? 
                            restaurant.getString("name") : food.getName() + "推荐餐厅");
                        data.put("longitude", restaurant.getBigDecimal("longitude"));
                        data.put("latitude", restaurant.getBigDecimal("latitude"));
                        data.put("address", restaurant.getString("address"));
                        data.put("coverImage", restaurant.getString("image") != null ? 
                            restaurant.getString("image") : food.getCoverImage());
                        data.put("type", "restaurant");
                        restaurantList.add(data);
                    }
                }
            }
        } catch (Exception e) {
            continue;  // JSON解析失败跳过
        }
    }
    
    return restaurantList;
}

// 获取酒店、文化地图数据（类似景点）...
```

### 3.3 地理编码实现

**地址转经纬度** ([MapServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/MapServiceImpl.java#L494-L568))：

```java
public Map<String, BigDecimal> geocodeAddress(String address, String city) {
    if (address == null || address.trim().isEmpty()) {
        return null;
    }
    
    if (mapApiKey == null || mapApiKey.isEmpty()) {
        return null;
    }
    
    try {
        String encodedAddress = URLEncoder.encode(address, StandardCharsets.UTF_8.toString());
        String url = String.format("https://restapi.amap.com/v3/geocode/geo?key=%s&address=%s", 
                                  mapApiKey, encodedAddress);
        
        // 添加城市限制
        if (city != null && !city.isEmpty()) {
            String encodedCity = URLEncoder.encode(city, StandardCharsets.UTF_8.toString());
            url += "&city=" + encodedCity;
        } else {
            url += "&city=太原";  // 默认城市
        }
        
        String response = restTemplate.getForObject(url, String.class);
        JSONObject jsonResponse = JSON.parseObject(response);
        
        if ("1".equals(jsonResponse.getString("status"))) {
            JSONArray geocodes = jsonResponse.getJSONArray("geocodes");
            if (geocodes != null && geocodes.size() > 0) {
                JSONObject geocode = geocodes.getJSONObject(0);
                String location = geocode.getString("location");
                if (location != null && location.contains(",")) {
                    String[] coords = location.split(",");
                    BigDecimal longitude = new BigDecimal(coords[0]);
                    BigDecimal latitude = new BigDecimal(coords[1]);
                    
                    Map<String, BigDecimal> result = new HashMap<>();
                    result.put("longitude", longitude);
                    result.put("latitude", latitude);
                    return result;
                }
            }
        }
    } catch (Exception e) {
        logger.error("地理编码失败", e);
    }
    
    return null;
}
```

### 3.4 路径规划实现

**多模式路径规划** ([MapServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/MapServiceImpl.java#L197-L312))：

```java
public Map<String, Object> planRoute(Double originLng, Double originLat, 
                                     Double destLng, Double destLat, String mode) {
    Map<String, Object> result = new HashMap<>();
    
    // 如果未配置API密钥，使用降级方案计算直线距离
    if (mapApiKey == null || mapApiKey.isEmpty()) {
        double distance = calculateDistance(originLat, originLng, destLat, destLng);
        result.put("distance", distance);
        result.put("distanceText", String.format("%.2f", distance) + "公里");
        
        // 根据模式估算时间
        int estimatedMinutes;
        if ("walking".equals(mode)) {
            estimatedMinutes = (int)(distance / 5.0 * 60);  // 步行5km/h
        } else if ("riding".equals(mode) || "bicycling".equals(mode)) {
            estimatedMinutes = (int)(distance / 15.0 * 60);  // 骑行15km/h
        } else {
            estimatedMinutes = (int)(distance / 50.0 * 60);  // 驾车50km/h
        }
        result.put("duration", estimatedMinutes + "分钟");
        result.put("mode", mode != null ? mode : "driving");
        result.put("message", "请配置地图API密钥以获取详细路线规划");
        return result;
    }
    
    // 调用高德地图API
    try {
        String routeType = "driving";
        if ("walking".equals(mode)) {
            routeType = "walking";
        } else if ("riding".equals(mode) || "bicycling".equals(mode)) {
            routeType = "bicycling";
        }
        
        String url = String.format(
            "https://restapi.amap.com/v3/direction/%s?key=%s&origin=%s,%s&destination=%s,%s&extensions=all",
            routeType, mapApiKey, originLng, originLat, destLng, destLat
        );
        
        String response = restTemplate.getForObject(url, String.class);
        JSONObject jsonResponse = JSON.parseObject(response);
        
        if ("1".equals(jsonResponse.getString("status"))) {
            JSONObject route = jsonResponse.getJSONObject("route");
            if (route != null) {
                JSONArray paths = route.getJSONArray("paths");
                if (paths != null && paths.size() > 0) {
                    JSONObject path = paths.getJSONObject(0);
                    int distance = path.getIntValue("distance");  // 米
                    int duration = path.getIntValue("duration");  // 秒
                    
                    result.put("distance", distance / 1000.0);
                    result.put("distanceText", String.format("%.2f", distance / 1000.0) + "公里");
                    result.put("duration", duration / 60 + "分钟");
                    result.put("durationSeconds", duration);
                    result.put("mode", mode != null ? mode : "driving");
                    
                    // 解析详细步骤
                    JSONArray stepsArray = path.getJSONArray("steps");
                    if (stepsArray != null && stepsArray.size() > 0) {
                        List<Map<String, Object>> stepsList = new ArrayList<>();
                        for (int i = 0; i < stepsArray.size(); i++) {
                            JSONObject step = stepsArray.getJSONObject(i);
                            Map<String, Object> stepMap = new HashMap<>();
                            stepMap.put("instruction", step.getString("instruction"));
                            stepMap.put("road", step.getString("road"));
                            stepMap.put("distance", step.getInteger("distance"));
                            stepsList.add(stepMap);
                        }
                        result.put("steps", stepsList);
                    }
                    
                    result.put("success", true);
                    return result;
                }
            }
        }
    } catch (Exception e) {
        logger.error("路径规划失败", e);
    }
    
    // API调用失败时的降级处理
    double distance = calculateDistance(originLat, originLng, destLat, destLng);
    result.put("distance", distance);
    result.put("distanceText", String.format("%.2f", distance) + "公里");
    // 估算时间...
    result.put("success", false);
    result.put("message", "路线规划失败，返回估算距离和时间");
    
    return result;
}

/**
 * 计算两点间距离（Haversine公式）
 */
private double calculateDistance(double lat1, double lon1, double lat2, double lon2) {
    final int R = 6371;  // 地球半径（公里）
    
    double latDistance = Math.toRadians(lat2 - lat1);
    double lonDistance = Math.toRadians(lon2 - lon1);
    
    double a = Math.sin(latDistance / 2) * Math.sin(latDistance / 2)
            + Math.cos(Math.toRadians(lat1)) * Math.cos(Math.toRadians(lat2))
            * Math.sin(lonDistance / 2) * Math.sin(lonDistance / 2);
    
    double c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    
    return R * c;
}
```

### 3.5 地点搜索实现

**POI搜索** ([MapServiceImpl.java](file:///e:/tourism/tourism-platform-backend/src/main/java/com/tourism/service/impl/MapServiceImpl.java#L314-L492))：

```java
public List<Map<String, Object>> searchPlace(String keyword, String city) {
    List<Map<String, Object>> result = new ArrayList<>();
    
    if (keyword == null || keyword.isEmpty()) {
        return result;
    }
    
    if (mapApiKey == null || mapApiKey.isEmpty() || restTemplate == null) {
        return result;
    }
    
    try {
        String encodedKeyword = URLEncoder.encode(keyword, StandardCharsets.UTF_8.toString());
        String url = String.format("https://restapi.amap.com/v3/place/text?key=%s&keywords=%s",
                                  mapApiKey, encodedKeyword);
        
        // 城市限制
        if (city != null && !city.isEmpty()) {
            String encodedCity = URLEncoder.encode(city, StandardCharsets.UTF_8.toString());
            url += "&city=" + encodedCity + "&citylimit=true";
        } else {
            url += "&city=太原&citylimit=true";
        }
        
        url += "&offset=20&page=1&extensions=all";
        
        String response = restTemplate.getForObject(url, String.class);
        JSONObject jsonResponse = JSON.parseObject(response);
        
        if ("1".equals(jsonResponse.getString("status"))) {
            JSONArray pois = jsonResponse.getJSONArray("pois");
            if (pois != null && pois.size() > 0) {
                // 太原市范围过滤
                double minLng = 112.0, maxLng = 113.0;
                double minLat = 37.4, maxLat = 38.2;
                
                for (int i = 0; i < pois.size(); i++) {
                    JSONObject poi = pois.getJSONObject(i);
                    String location = poi.getString("location");
                    
                    if (location != null && location.contains(",")) {
                        String[] coords = location.split(",");
                        double lng = Double.parseDouble(coords[0]);
                        double lat = Double.parseDouble(coords[1]);
                        
                        // 过滤太原范围外的结果
                        boolean inRange = (lng >= minLng && lng <= maxLng && 
                                          lat >= minLat && lat <= maxLat);
                        
                        // 城市名称匹配
                        boolean cityMatch = false;
                        String poiCity = poi.getString("cityname");
                        String poiAdname = poi.getString("adname");
                        if (city != null && !city.isEmpty()) {
                            cityMatch = (poiCity != null && (poiCity.equals(city) || poiCity.contains(city + "市"))) 
                                        || (poiAdname != null && poiAdname.contains(city));
                        }
                        
                        if ((city != null && !city.isEmpty() && cityMatch) || inRange) {
                            Map<String, Object> place = new HashMap<>();
                            place.put("id", poi.getString("id"));
                            place.put("name", poi.getString("name"));
                            place.put("address", poi.getString("address"));
                            place.put("longitude", new BigDecimal(lng));
                            place.put("latitude", new BigDecimal(lat));
                            place.put("city", poiCity);
                            place.put("adname", poiAdname);
                            
                            // 获取图片
                            try {
                                if (poi.containsKey("photos") && poi.get("photos") != null) {
                                    JSONArray photos = poi.getJSONArray("photos");
                                    if (photos != null && photos.size() > 0) {
                                        JSONObject photo = photos.getJSONObject(0);
                                        place.put("image", photo.getString("url"));
                                    }
                                }
                            } catch (Exception e) {
                                // 忽略图片
                            }
                            
                            result.add(place);
                        }
                    }
                }
            }
        }
    } catch (Exception e) {
        logger.error("地点搜索失败", e);
    }
    
    return result;
}
```

---

## 四、设计亮点总结

### 4.1 评论模块亮点

1. **多态设计**：通过 `targetType` + `targetId` 支持多种内容类型
2. **冗余统计**：评分和评论数直接存储在内容表，避免关联查询
3. **级联操作**：删除评论时同时删除回复并调整统计数
4. **权限控制**：用户只能删除自己的评论

### 4.2 AI模块亮点

1. **提示词工程**：精心设计的系统提示词，包含平台数据作为上下文
2. **动态数据注入**：检测用户询问特定酒店时，动态查询并注入详细信息
3. **降级处理**：JSON解析失败时有容错机制
4. **配置化管理**：提示词模板、API参数可配置

### 4.3 地图模块亮点

1. **统一数据接口**：整合多种类型的地理位置数据
2. **智能地址解析**：支持地址自动转经纬度
3. **降级策略**：API不可用时使用Haversine公式计算直线距离
4. **范围过滤**：地点搜索时过滤目标城市范围外的结果
